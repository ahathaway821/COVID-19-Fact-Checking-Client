import React from "react";

class Home extends React.Component {
    // props are icon, label, action, disabled
    render() {
        return (
            <div>
                <h2>Home Page</h2>
            </div>
        );
    }
}

export default Home;